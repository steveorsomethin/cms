Clean up/expand expect.js definitions. Document should do something other than test for object. Add support for dates, email addresses, other common types.
Completely fill out CRUD operations in domain objects.
Write unit tests for everything.
Write integration tests for end-to-end sanity checks.
Write regression tests to make sure changes don't negatively impact performance/stability.
Make http endpoints return proper response codes and helpful errors. Return stack traces when in dev mode
Look into forking http processes and fail gracefully.