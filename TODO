Write unit tests for everything.
Write integration tests for end-to-end sanity checks.
Write regression tests to make sure changes don't negatively impact performance/stability.
Look into forking http processes and fail gracefully.
Automate server setup. Generate public/private ssh keys for easier/more secure automation.